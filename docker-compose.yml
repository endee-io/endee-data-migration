version: "3.8"

services:
  migration:
    image: vector-migration:latest
    env_file: .env
    volumes:
      - ./data:/app/data
    networks:
      - vector-net

networks:
  vector-net:
    external: true